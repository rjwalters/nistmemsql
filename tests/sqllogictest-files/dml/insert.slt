# INSERT operations

statement ok
CREATE TABLE test (a INTEGER, b VARCHAR(20))

# Single row insert
statement ok
INSERT INTO test VALUES (1, 'one')

query IT
SELECT * FROM test
----
1 one

# Multi-row insert
statement ok
INSERT INTO test VALUES (2, 'two'), (3, 'three')

query I
SELECT COUNT(*) FROM test
----
3

# INSERT with column list
statement ok
INSERT INTO test (b, a) VALUES ('four', 4)

query IT
SELECT * FROM test WHERE a = 4
----
4 four

statement ok
DROP TABLE test
