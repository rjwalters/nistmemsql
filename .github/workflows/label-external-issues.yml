name: Label external issues

on:
  push:
    paths:
      - '.github/workflows/label-external-issues.yml'
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "noop"
