name: Label external issues

on:
  issues:
    types: [opened]
  push:
    paths:
      - '.github/workflows/label-external-issues.yml'

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "noop"
