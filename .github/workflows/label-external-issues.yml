name: Label external issues

on:
  push:
    paths:
      - '.github/workflows/label-external-issues.yml'

permissions:
  issues: write
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "noop"
