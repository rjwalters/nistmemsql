[build]
rustc-wrapper = "sccache"

[alias]
# Coverage commands using cargo-llvm-cov
coverage = "llvm-cov --workspace --html"
coverage-lcov = "llvm-cov --workspace --lcov --output-path target/coverage/lcov.info"
coverage-clean = "llvm-cov clean"
