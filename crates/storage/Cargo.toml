[package]
name = "storage"
version.workspace = true
edition.workspace = true

[dependencies]
types = { path = "../types" }
catalog = { path = "../catalog" }
ast = { path = "../ast" }

[dev-dependencies]
